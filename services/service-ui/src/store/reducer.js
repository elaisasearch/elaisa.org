const initialState = {
    loggedIn: false,
    email: "frodo.beutlin@hobbits.com",
    firstname: "Frodo",
    lastname: "Beutlin"
}

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;